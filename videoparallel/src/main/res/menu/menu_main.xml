<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/addImage1"
        android:title="Add Image"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_image"/>

    <item android:id="@+id/addImage"
    android:title="Add Image"
    app:showAsAction="always"
    android:icon="@drawable/ic_gallery_image"/>

    <item android:id="@+id/Video"
        app:showAsAction="always"
        android:title="Video"
        android:icon="@drawable/ic_video_image"
        />
    <item android:id="@+id/history"
        android:title="History"
        />


</menu>